import React from 'react'
import SeatPickerContainer from '../containers/SeatPickerContainer'

const App = (props) => {
  return (
    <div>
      <SeatPickerContainer />
    </div>
  );
}

export default App
